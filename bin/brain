#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

case "${1:-}" in
  add)
    shift
    exec node "$DIR/scripts/add.js" "$@"
    ;;
  *)
    echo 'usage: brain add <url | file path | "note text">' >&2
    exit 1
    ;;
esac
